#include <stdio.h>
#include "main.h"

/**
 * puts_half - function
 *
 * @str: pointer
 *
 * Return: Always 0
 */

void puts_half(char *str)
{
	int a;
	int length;
	int half_length = length / 2;


	for (a = 0 ; a < half_length; a++)
		_putchar(s[a]);
	_putchar('\n');
}

