#include <stdio.h>
#include "main.h"
#include <string.h>

/**
 * _strlen - fonctions for pointers
 *
 * @s: input
 *
 * Return: Always return 0
 */

int _strlen(char *s)
{
	int a;
	int length;

	length = 0;

	for (a = 0 ; s[a] != '\0' ; i++)
		lenght++;

	return (length);
}
