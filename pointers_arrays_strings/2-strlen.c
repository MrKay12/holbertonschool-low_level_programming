#include <stdio.h>
#include "main.h"
#include <string.h>

/**
 * _strlen - fonctions for pointers
 *
 * @s: input
 *
 * Return: Always return 0
 */

int _strlen(char *s)
{
	int a = strlen(*s);

	_putchar("Length of %s = %d\n", name, length);
	return (0);
}
