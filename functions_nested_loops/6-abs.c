#include <stdio.h>
#include "main.h"
#include <stdlib.h>

/**
* _abs - Entry point
* @a: is an input
* Return: Always 0 (Success)
*/

int _abs(int a);
{
	if (a > 0)
	{
		return (a);
	}

	else
	{
		return (-a);
	}
}
